// Standalone test runner for AST target libraries
std := import('std')
test := import('test')

t := test.new('AST targets')

runner := import('ast-targets.test')
runner.run(t)

t.reportFailed()
t.exit()
