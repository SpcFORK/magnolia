{
	slice: slice
	default: default
} := import('std')

gui := import('GUI')

cs Backdrop(color?) {
    color := color? |> default(gui.rgb(0, 0, 0))
    draw := fn(window) gui.fillRect(window, 0, 0, window.width, window.height, color)
}