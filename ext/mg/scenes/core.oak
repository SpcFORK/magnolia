std := import('std')

introScene := import('scenes/intro')
menuScene := import('scenes/menu')

// ---

cs SceneModule(current?) {
	current := current? |> std.default(introScene)

	intro := introScene
	menu := menuScene
}