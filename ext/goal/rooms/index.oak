villageRoom := import('rooms/village')
cabinRoom := import('rooms/cabin')
shrineRoom := import('rooms/shrine')
cryptRoom := import('rooms/crypt')

fn buildRooms() {
	{
		village: villageRoom.room()
		cabin: cabinRoom.room()
		shrine: shrineRoom.room()
		crypt: cryptRoom.room()
	}
}
