cs Enemy(name, x, y, size, hp, speed, minX, maxX) {
	{
		name: name
		x: x
		y: y
		size: size
		hp: hp
		maxHp: hp
		speed: speed
		minX: minX
		maxX: maxX
		dir: 1
		alive: true
		xpReward: 4
		contactDamage: 2
	}
}