// libemail is a unified facade for email protocol libraries.
// Re-exports email-smtp, email-pop, and email-imap under a single import.

smtp := import('email-smtp')
pop := import('email-pop')
imap := import('email-imap')
